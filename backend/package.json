{
  "name": "hospital-feedback-backend",
  "version": "1.0.0",
  "description": "Backend API for hospital feedback system",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_OPTIONS=--dns-result-order=ipv4first && node server.js",
    "dev": "set NODE_OPTIONS=--dns-result-order=ipv4first && nodemon server.js",
    "setup": "node setup-mongodb.js",
    "sample-data": "node add-sample-data.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hospital",
    "feedback",
    "api",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.0.3",
    "multer": "^2.1.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
